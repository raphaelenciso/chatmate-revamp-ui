export interface IUserContact {
  id: string;
  username: string;
  email: string;
  avatar: string;
  // status: 'online' | 'away' | 'busy' | 'offline';
  // lastMessage?: string;
  // unreadCount?: number;
  // lastSeen?: string;
  // isAI?: boolean;
}
